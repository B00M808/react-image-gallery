import React from 'react';
import Nav from './Nav';
import SearchBar from './SearchBar';

const Header = () => (
    <header>
       <SearchBar />
       <Nav />
    </header>
);

export default Header;